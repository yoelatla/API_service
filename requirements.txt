fastapi==0.139.0        # web framework your app is built on — it takes incoming HTTP requests and routes them to your Python functions 
uvicorn[standard]==0.51.0  # ASGI server that runs FastAPI
sqlalchemy==2.0.51      # ORM
psycopg2-binary==2.9.12 # Postgres driver used by SQLAlchemy
python-dotenv==1.2.2    # loads .env into environment variables
pydantic==2.13.4        # request/response validation (schemas.py)
alembic==1.18.5         # database migrations
pytest==9.1.1           # test runner
httpx==0.28.1           # HTTP client used by FastAPI's TestClient
debugpy==1.8.21         # VS Code debugger support
